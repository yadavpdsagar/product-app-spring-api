spring.application.name=seller-app

# ************************* MySQL Database Configuration ******************************
spring.datasource.url=jdbc:mysql://localhost:3306/gitpull

spring.datasource.username=root
spring.datasource.password=sagar4260@
spring.jpa.hibernate.ddl-auto=create
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

file.upload-dir=D:\FINAYEARPROJECT\backup\productfor

# Ensure Spring Boot runs the schema and data SQL files at startup
#spring.datasource.initialization-mode=always
#
## This ensures that data.sql is executed during startup
#spring.datasource.data=classpath:data.sql
spring.jpa.show-sql=true

server.port=8080